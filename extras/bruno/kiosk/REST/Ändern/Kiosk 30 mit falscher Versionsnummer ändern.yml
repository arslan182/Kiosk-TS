info:
  name: Kiosk 30 mit falscher Versionsnummer ändern
  type: http
  seq: 2

http:
  method: PUT
  url: "{{restUrl}}/:id"
  headers:
    - name: If-Match
      value: "0"
  params:
    - name: id
      value: "30"
      type: path
  body:
    type: json
    data: |-
      {
        "name": "Super Kiosk Am Eck",
        "email": "kontakt@superkiosk.de",
        "istGeoeffnet": true,
        "homepage": "https://superkiosk.de",
        "username": "admin_kiosk"
      }
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "412"
    - expression: res.headers['content-type']
      operator: eq
      value: application/problem+json
    - expression: res.body.statusCode
      operator: eq
      value: "412"
    - expression: res.body.detail
      operator: contains
      value: Versionsnummer

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5